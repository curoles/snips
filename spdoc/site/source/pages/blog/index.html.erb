---
title: Blog Index
pageable: true
per_page: 10
---

<!--copy of https://github.com/middleman/middleman-templates-blog/blob/master/template/source/index.html.erb-->

<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<% if false %>
<!--% page_articles.each_with_index do |article, i| %-->
  <!--h3><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span>
      <span>by <%= article.metadata[:page][:user] %> in category <%= article.metadata[:page][:category] %>
  </h3-->
  <!-- use article.summary(250) if you have Nokogiri available to show just
       the first 250 characters -->
  <!--%= article.body %-->
<!--% end %>
<% end %>

<table class="table table-striped">
    <tr> <th>#</th> <th>Title</th> <th>Date</th> <th>Author</th> <th>Category</th> </tr>
<% page_articles.each_with_index do |article, i| %>
    <tr> <td><%= i %></td> 
         <td><%= link_to article.title, article %></td>
         <td><%= article.date.strftime('%b %e') %></td>
         <td><%= article.metadata[:page][:user] %> </td>
         <td><%= article.metadata[:page][:category] %></td>
     </tr>
<% end %>
</table>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
